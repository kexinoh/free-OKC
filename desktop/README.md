# OKCVM Desktop

OKCVM 的跨平台桌面应用，基于 [Tauri](https://tauri.app/) 构建。

## 特性

- 🚀 **轻量高效** - 安装包约 15MB，内存占用低
- 🔒 **安全可靠** - 细粒度权限控制，最小化系统访问
- 🖥️ **原生体验** - 系统托盘、全局快捷键、原生通知
- 🔄 **自动更新** - 内置更新器，保持最新版本
- 🌙 **深色模式** - 跟随系统主题自动切换
- 📁 **文件集成** - 原生文件对话框，拖放支持

## 系统要求

| 平台 | 最低版本 |
|------|----------|
| macOS | 10.15 (Catalina) |
| Windows | 10 (1803) |
| Linux | Ubuntu 20.04+ |

## 开发环境设置

### 前置条件

1. **Rust 工具链**
   ```bash
   curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
   ```

2. **Node.js 18+**
   ```bash
   # 使用 nvm 安装
   nvm install 18
   nvm use 18
   ```

3. **Tauri CLI**
   ```bash
   cargo install tauri-cli
   ```

4. **Python 3.9+** (用于后端打包)
   ```bash
   pip install pyinstaller
   ```

### 安装依赖

```bash
cd desktop
npm install
```

### 开发模式

```bash
# 启动开发服务器（热重载）
npm run tauri dev
```

### 生产构建

```bash
# 构建安装包
npm run tauri build
```

构建产物位于 `src-tauri/target/release/bundle/`

## 项目结构

```
desktop/
├── src-tauri/              # Tauri Rust 源码
│   ├── src/
│   │   ├── main.rs         # 应用入口
│   │   ├── commands/       # IPC 命令
│   │   ├── sidecar/        # Python 后端管理
│   │   ├── system/         # 系统集成（托盘、快捷键）
│   │   └── state/          # 应用状态
│   ├── tauri.conf.json     # Tauri 配置
│   └── icons/              # 应用图标
├── src/                    # 前端适配层
│   ├── adapter/            # 原生 API 桥接
│   └── patches/            # 前端补丁
├── scripts/                # 构建脚本
└── backend-bundle/         # Python 打包配置
```

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Cmd/Ctrl+Shift+K` | 显示/隐藏窗口 |
| `Cmd/Ctrl+Shift+N` | 新建对话 |
| `Cmd/Ctrl+,` | 打开设置 |
| `Cmd/Ctrl+Q` | 退出应用 |

## 相关文档

- [桌面端设计规范](../spec/desktop.md)
- [架构文档](../docs/architecture.md)
- [安全说明](../security.md)

## 许可证

[MIT License](../LICENSE)
