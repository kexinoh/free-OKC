# This directory holds the bundled Python backend executables.
# Files are generated during the build process and should not be committed.
#
# The naming convention for sidecar binaries is:
#   okcvm-server-{target-triple}[.exe]
#
# Examples:
#   - okcvm-server-x86_64-apple-darwin
#   - okcvm-server-aarch64-apple-darwin
#   - okcvm-server-x86_64-pc-windows-msvc.exe
#   - okcvm-server-x86_64-unknown-linux-gnu
